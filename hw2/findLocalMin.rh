
fun findLocalMin(array, arraysize)
{
    #write func





    return -1; 
}




#main function 
fun main()
{
    let arr= [9,7,7,2,1,3,7,5,4,7,3,3,4,8,6,9]

    # get size of array 
    let arraysize = len(arr); 

    #call the func 
    let localMinIndex = findLocalMin(arr,arraysize); 

    #print the result 
    print("One of the local minima is located at..."); 
    print("Index: ", localMinIndex); 
    print("Value: ", arr[localMinIndex]); 

    print("Hello World" ); 

}